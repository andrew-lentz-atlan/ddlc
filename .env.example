# DDLC — Atlan App Framework environment variables
# Copy this to .env and fill in your values.
# In production, these are injected by Dapr secret store via Helm.

# Required — Atlan tenant connection
ATLAN_BASE_URL=https://your-tenant.atlan.com
ATLAN_API_KEY=your-api-key-here

# Optional — Snowflake account name for demo data servers (e.g. "your-account")
# Defaults to "your-account" placeholder if not set
ATLAN_SNOWFLAKE_ACCOUNT=

# Optional — dbt Cloud integration (enables "Push to dbt Cloud" button)
DBT_CLOUD_API_KEY=
DBT_CLOUD_ACCOUNT_ID=
DBT_CLOUD_JOB_ID=

# SDK internals (usually auto-set by Dapr sidecar in production)
# APPLICATION_MODE=LOCAL   # LOCAL | WORKER | SERVER
# TEMPORAL__HOST_URL=localhost:7233
